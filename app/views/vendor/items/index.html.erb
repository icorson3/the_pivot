<% if @vendor.status == "approved" %>
  <%= render partial: "shared/item_list", locals: {title: @vendor.name} %>
<% else %>
  <div class="text-center">
    <h1>We're Sorry, but this store has been retired</h1>
  </div>
<% end %>
