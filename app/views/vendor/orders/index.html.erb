<h1><%= @vendor.name %>'s Orders</h1>

<table class = "table" id="vendor-order-table">
  <th>Name</th>
  <% @order_items.each do |order_item| %>
  <tr><td><%= order_item.item.name %></td></tr>
  <% end %>
</table>
